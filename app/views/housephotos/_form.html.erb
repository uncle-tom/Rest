<%= form_for(@housephoto) do |form| %>
  <% if housephoto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(housephoto.errors.count, "error") %> prohibited this housephoto from being saved:</h2>

      <ul>
      <% housephoto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.file_field :houseimg, multiple: true, :required => true  %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
